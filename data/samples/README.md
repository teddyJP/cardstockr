This folder is for sample and development CSVs, such as eBay "Sold" exports.

For v1, you can:

- Export sold listings from eBay as CSV.
- Drop them into this directory.
- Run `python -m scripts.ingest_sales` from the `backend/` folder to ingest them.

